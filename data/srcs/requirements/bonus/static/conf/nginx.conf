events {}

http {
    include mime.types;

    server {
        listen 80;
        server_name astefane.42.fr;

        root /var/www/static;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}